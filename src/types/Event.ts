// src/types/Event.ts (New file)
export interface Event {
    id: number;
    event_name: string;
    description: string;
    location: string;
    event_date: string;
    date_entered: string;
    visible: boolean;
  }